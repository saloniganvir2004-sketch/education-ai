from main import generate_answer

question = "What is Artificial Intelligence?"

print(generate_answer(
    question=question,
    user_id="test_user"
))
print(generate_answer(
    question=question,
    user_id="test_user"
))